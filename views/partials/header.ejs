<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="shortcut icon" href="/icons/siteicon.ico" type="image/icon">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/styles/main.css">

  <title><%= title %></title>
</head>
<body>
  <header class="text-bg-dark p-3">
    <div class="container">
      <div class="d-flex flex-wrap align-items-center justify-content-center justify-content-lg-start">
        <div class="nav col-6 justify-content-start mb-md-0">
          <% if (isLoggedIn) { %>
          <a href="/user"><img src="/icons/main_white.svg" alt="" width="36" height="36"></a>
          <% } else { %>
            <a href="/"><img src="/icons/main_white.svg" alt="" width="36" height="36"></a>
          <% } %>
        <!--Затем сделать дроп даун вместо этого
        <ul class="nav col-6 col-lg-auto px-3 me-lg-auto mb-2 justify-content-center mb-md-0">
          <li><a href="/posts" class="nav-link text-white">Posts</a></li>
          <li><a href="/pricing" class="nav-link text-white">Pricing</a></li>
          <li><a href="/comunity" class="nav-link text-white">Comunity</a></li>
        </ul>
        -->
        </div>
       
        <% if (isLoggedIn) { %>
          <div class="nav col-6 justify-content-end mb-md-0">
            <a href="/logout" type="submit" class="btn btn-warning px-2 mx-2">Sign Out</a>
          </div>
        <% } else { %>
          <div class="nav col-6 justify-content-end mb-md-0">
            <a href="/login" type="submit" class="btn btn-light px-3 mx-2">Login</a>
            <a href="/signup" type="submit" class="btn btn-warning px-2 mx-2">Sign Up</a>
          </div>
        <% } %> 
        
      </div>
    </div>
  </header>
  <main>